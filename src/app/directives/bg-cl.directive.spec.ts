import { BGCLDirective } from './bg-cl.directive';

describe('BGCLDirective', () => {
  it('should create an instance', () => {
    const directive = new BGCLDirective();
    expect(directive).toBeTruthy();
  });
});
